name: Secure Registry Auth Cache Runner

on:
  push:
    branches:
      - registry-auth-cache-secure-20260925
    paths:
      - .github/secure-trigger/start.txt

permissions:
  contents: write

jobs:
  secure-probe:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: linux
            os: ubuntu-latest
    runs-on: ${{ matrix.os }}
    timeout-minutes: 12
    steps:
      - name: Checkout runner repo
        uses: actions/checkout@v4
        with:
          ref: registry-auth-cache-secure-20260925
          fetch-depth: 0
          persist-credentials: true

      - name: Run encrypted probe executor
        shell: pwsh
        run: ./.github/secure-runner/runner.ps1 -Target "${{ matrix.target }}"

      - name: Upload encrypted result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: secure-result-${{ matrix.target }}
          path: ${{ runner.temp }}/secure-result/
          retention-days: 1
