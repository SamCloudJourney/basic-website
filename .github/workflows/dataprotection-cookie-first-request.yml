name: DataProtection built-in repository first-request matrix

on:
  push:
    branches:
      - research/dataprotection-cookie-first-request-20260921

permissions:
  contents: read

jobs:
  supported:
    strategy:
      fail-fast: false
      matrix:
        include:
          - tfm: '8.0'
            mode: safe
          - tfm: '9.0'
            mode: stale
          - tfm: '10.0'
            mode: stale
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '${{ matrix.tfm }}.x'
      - name: Retarget app
        working-directory: research/dataprotection-cookie-first-request
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path("dataprotection-cookie-first-request.csproj")
          p.write_text(p.read_text().replace("<TargetFramework>net10.0</TargetFramework>", "<TargetFramework>net${{ matrix.tfm }}</TargetFramework>"))
          PY
      - name: Validate first request after revocation
        working-directory: research/dataprotection-cookie-first-request
        shell: bash
        run: |
          set -euxo pipefail
          dotnet --info
          if [ "${{ matrix.mode }}" = "safe" ]; then
            dotnet run -c Release -- --expect-safe | tee result.log
            grep -F 'FIRST_POST_REVOKE_COOKIE_REJECTED=True' result.log
            grep -F 'BUILTIN_REPOSITORY_SAFE_CONTROL=PASS' result.log
          else
            dotnet run -c Release | tee result.log
            grep -F 'FIRST_POST_REVOKE_COOKIE_ACCEPTED=CONFIRMED' result.log
            grep -F 'SECOND_POST_REVOKE_COOKIE_REJECTED=True' result.log
            grep -F 'BUILTIN_REPOSITORY_FIRST_REQUEST_WINDOW=PASS' result.log
          fi

  rc1:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '11.0.100-rc.1.26425.128'
      - name: Retarget app
        working-directory: research/dataprotection-cookie-first-request
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p=Path("dataprotection-cookie-first-request.csproj")
          p.write_text(p.read_text().replace("<TargetFramework>net10.0</TargetFramework>", "<TargetFramework>net11.0</TargetFramework>"))
          PY
      - name: Validate first request after revocation
        working-directory: research/dataprotection-cookie-first-request
        shell: bash
        run: |
          set -euxo pipefail
          dotnet --info
          dotnet run -c Release | tee result.log
          grep -F 'FIRST_POST_REVOKE_COOKIE_ACCEPTED=CONFIRMED' result.log
          grep -F 'SECOND_POST_REVOKE_COOKIE_REJECTED=True' result.log
          grep -F 'BUILTIN_REPOSITORY_FIRST_REQUEST_WINDOW=PASS' result.log
