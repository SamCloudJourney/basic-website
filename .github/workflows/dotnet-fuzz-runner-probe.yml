name: Runner probe
on:
  push:
    branches: [dotnet-fuzz-public-run-20260924]
permissions:
  contents: read
jobs:
  probe:
    runs-on: windows-latest
    steps:
      - shell: pwsh
        run: |
          Write-Host "RUNNER_OK"
          dotnet --info
